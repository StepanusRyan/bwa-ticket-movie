<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stepanusryan.bwamovie">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" /> <!-- <uses-permission-sdk-23 android:name="android.permission.INTERNET"/> -->
    <!-- <uses-permission-sdk-23 android:name="android.permission.ACCESS_WIFI_STATE"/> -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BWAMovie">
        <activity android:name=".wallet.TopUpSuccessActivity"/>
        <activity android:name=".wallet.TopUpActivity" />
        <activity android:name=".main.ui.ticket.TicketActivity" />
        <activity android:name=".wallet.MyWalletActivity" />
        <activity android:name=".main.ui.profile.EditProfileActivity" />
        <activity android:name=".checkout.CheckoutSuccessActivity" />
        <activity android:name=".checkout.CheckoutActivity" />
        <activity android:name=".pilihbangku.ChooseSeatActivity" />
        <activity android:name=".detailmovie.DetailMovieActivity" />
        <activity
            android:name=".main.MainActivity"
            android:label="@string/title_activity_main" />
        <activity android:name=".signup.PhotoScreenActivity" />
        <activity android:name=".signup.SignUpActivity" />
        <activity android:name=".signin.SignInActivity" />
        <activity android:name=".onboarding.OnBoardingThreeActivity" />
        <activity android:name=".onboarding.OnBoardingTwoActivity" />
        <activity android:name=".onboarding.OnBoardingOneActivity" />
        <activity
            android:name=".splashscreen.SplashScreenActivity"
            android:theme="@style/Theme.SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>